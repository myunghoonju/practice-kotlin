name: deploy-practice-kotlin

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: hello world
        run: echo "hello world!"

      - name: goodbye world
        run: |
          echo "good"
          echo "bye"
