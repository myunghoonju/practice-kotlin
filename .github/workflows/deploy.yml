name: deploy-practice-kotlin

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: hello world
        run: echo "hello world!"

      - name: multiple steps(|)
        run: |
          echo "good"
          echo "bye"

      - name: variable
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REPOSITORY
